/**
 * Intended as reference for implementations - NOT INTENDED FOR USE.
 */
class IGuestRepo {
  constructor({ dbContext }) {}
  getGuests() {}

  getGuestById(id) {}

  /** TODO: whenever the next assignment asks for POST & DELETE, add those*/
}
